---
galaxy_info:
  description: Ansible role to deploy Prometheus nginx exporter
  company: Personal project
  role_name: ansible_prometheus_nginx_exporter
 

  license: Apache

  min_ansible_version: 2.8

  platforms:
    - name: Ubuntu
      versions:
        - focal

  galaxy_tags: [prometheus, monitoring]

dependencies: []
# List your role dependencies here, one per line. Be sure to remove the '[]' above,
# if you add dependencies to this list.
